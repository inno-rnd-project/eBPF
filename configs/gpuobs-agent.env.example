# metrics/health listen address (기본값 :9820)
LISTEN_ADDR=:9820

# 관찰 대상 노드 이름 (비워두면 hostname 사용)
NODE_NAME=

# NVML device 폴링 주기 (기본값 5s, >0 이어야 함)
GPU_POLL_INTERVAL=5s

# per-device gpuobs_device_* 메트릭 발행 여부 (기본값 true)
GPU_METRICS_ENABLED=true
