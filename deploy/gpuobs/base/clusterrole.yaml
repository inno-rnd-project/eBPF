apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: gpuobs-agent
  labels:
    app.kubernetes.io/name: gpuobs-agent
    app.kubernetes.io/part-of: ebpf-project
    app.kubernetes.io/component: gpu-observer
# Phase 1은 placeholder collector로 Kubernetes API를 호출하지 않으므로 rules를 비워둔다.
# Phase 3에서 Pod 귀속(ResolvePID) 도입 시 pods/nodes에 대한 get/list/watch 권한이 추가된다.
rules: []
