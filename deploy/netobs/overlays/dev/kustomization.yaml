apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ebpf-project

resources:
  - ../../base

images:
  - name: netobs-agent
    newTag: "1.0.6"

patches:
  - path: patch-daemonset.yaml
