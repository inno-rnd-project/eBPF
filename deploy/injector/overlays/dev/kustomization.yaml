apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ebpf-project

resources:
  - ../../base

images:
  - name: workload-injector
    newTag: "0.4.8"

patches:
  - path: patch-job.yaml
