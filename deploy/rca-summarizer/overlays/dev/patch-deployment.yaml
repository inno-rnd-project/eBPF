apiVersion: apps/v1
kind: Deployment
metadata:
  name: rca-summarizer
  namespace: ebpf-project
spec:
  template:
    spec:
      containers:
        - name: rca-summarizer
          # #312 후속: dev 도 ghcr 를 이미지 소스로 쓴다 (워커 스케줄 시 로컬 이미지 부재 대응).
          imagePullPolicy: IfNotPresent
